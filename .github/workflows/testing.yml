# # This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# # For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

# name: Node.js CI

# on:
#   push:
#     branches: [testing]
#   pull_request:
#     branches: [testing]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [20.x]
#         # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

#     steps:
#       - uses: actions/checkout@v2
#       - name: Use Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v2
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: 'npm'
#       - run: rm -rf node_modules
#       - run: npm install
#       - run: npm ci

#   deploy:
#     needs: [build]
#     runs-on: ubuntu-latest

#     steps:
#       - name: ssh deploy
#         uses: appleboy/ssh-action@master
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ${{ secrets.SSH_USER }}
#           password: ${{ secrets.SSH_PASSWORD }}   # Gunakan password dari secrets
#           key: ${{ secrets.SSH_PRIVATE_KEY }}     # Gunakan private key dari secrets
#           command_timeout: 200m
#           script: |
#             cd /var/www/www.testapi.al-arabi.sch.id/api_alarabi
          
#             sudo git pull
#             sudo rm -rf node_modules
#             sudo npm install
#             sudo npx prisma db push
#             pm2 stop testapi
#             pm2 delete testapi
#             pm2 start src/index.js --name testapi
#             pm2 save